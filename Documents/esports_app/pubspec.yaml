name: esports_app
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Rango de SDK más flexible

dependencies:
  flutter:
    sdk: flutter

  # --- DEPENDENCIAS REQUERIDAS ---
  http: ^1.2.1
  shared_preferences: ^2.2.3
  # (Quitamos 'intl' para siempre)
  # --------------------------------

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0 

flutter:
  uses-material-design: true